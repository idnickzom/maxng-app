/api/movies
/api/comment/?movie_id={movie_id}&comment={comment}
/api/comments/
/api/comments/movies/:movie_id
/api/characters/?page={page}&sort_type={sort_type}&order={order}&gender={gender}&search={search}
    page: page no of the character list (people)
    sort_type: name or height
    order: asc, desc
    gender: male, female, n/a (optional)
    search: any keyword (optional)